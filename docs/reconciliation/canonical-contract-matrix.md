# Canonical Contract Matrix — `nabhold/shared` ↔ `nabhold/baobab-cp`

Companion to `shared-control-plane-audit.md`. One row per canonical concept that
`nabhold/shared` defines (via an accepted ADR and/or a `contracts/control-plane/v1/*`
schema) and that `baobab-cp` implements or is expected to implement. "Shared source" cites
the accepted contract; concepts that exist only in the *Proposed* `Baobab Canonical Mapping
Model.md` document are marked as such and are not treated as binding for status purposes.

Legend — **Naming**: Match / Partial / Mismatch. **Test status**: Covered (real Postgres or
meaningful unit test) / Partial (mocked/in-memory only) / None.

| Canonical term | Shared source | Control Plane representation | DB representation | REST representation | Event representation | Naming | Semantic status | Test status | Action |
|---|---|---|---|---|---|---|---|---|---|
| **Tenant** | ADR-0003; `tenancy.yaml`; `tenant-registration.schema.json`; `context-resolution.schema.json` | `domain.Tenant`, `domain.RegisterTenant` (`internal/domain/tenant.go`) | `tenants` + `legal_entities` tables — defined **only** in `000001_control_plane.up.sql` / `000002_audit_identity.up.sql` / `000003_context_resolution_audit.up.sql`, which `migrate.go`'s `canonicalMigrationNames` never applies | `POST /v1/tenants`, `GET /v1/tenants/{id}`, lifecycle actions (`api/router.go`) | none | Partial — field names match the contract; `tenant_id`/`legal_entity_id` format grammar not enforced | **Broken at runtime**: implemented against tables the migration runner never creates (audit §2.1) | Partial — unit tests on `domain.RegisterTenant.Validate` only; no integration test runs migrations + store together | P0: apply/renumber migrations (done, see commit); add end-to-end integration test (backlog) |
| **Legal Entity** | ADR-0001 §2.2 (shared); ADR-0003 §2.3; `contracts/legal-entity/registry.yaml` | referenced by `owner_tenant_id`/`legal_entity_id` fields across domain types | `legal_entity_id text` (free text, no FK to a registry table, no case constraint) | accepted as input to `POST /v1/tenants` | none | **Mismatch** — `RegisterTenant.Validate()` applies the lowercase `resourceID` pattern to `legal_entity_id`, rejecting the canonical uppercase-kebab form (`THAMANI-GLOBAL`) that `nabhold/shared`'s registry actually uses | Non-conformant; registry membership never checked | None | P1: fix validation regex; consider embedding/generating the registry as a Go const set for membership checks |
| **CanonicalEntity** | `canonical-mapping.schema.json` (implicitly, via `canonical_entity_id` references); `domain.schema.json`; Mapping Model §7 (Proposed, informative) | `domain.CanonicalEntity` (`internal/domain/canonical.go`); `service.CanonicalEntityService` | `registry.canonical_entity` (migration `000002_canonical_registry.sql` + `000018` version column) | `POST/GET /v1/canonical-entities`, lifecycle sub-resources (`api/canonical_handler.go`) | none | Partial — `canonical_key`/`entity_type`/`status` match; DB column is `external_key` where the Go struct and shared schema both say `canonical_key` | No temporal validity columns (`effective_from`/`effective_to`) despite lifecycle states requiring them | Partial — `canonical_test.go` unit tests only | P1: rename `external_key`→`canonical_key` in schema, add temporal columns |
| **ExternalReference** | `canonical-mapping.schema.json` §`externalReference` | not modelled as a distinct Go type; folded into `Mapping.ExternalReferenceID` | `registry.external_reference` (`000010`) — columns `provider`/`provider_key` instead of shared's `system_namespace`/`engine_id`/`native_entity_type`/`native_id` | none exposed directly | none | Mismatch — different field set entirely, no engine/environment/fingerprint tracking | Distinct-from-Mapping principle (Mapping Model §8.4) is honoured structurally (separate table) but field coverage is materially smaller than the shared schema | None | P1/P2: align columns with `canonical-mapping.schema.json`'s `externalReference` definition once §10.1 lands |
| **Mapping** | `canonical-mapping.schema.json` §`mapping`; ADR-0004 (ID grammar) | `domain.Mapping` (`internal/domain/canonical.go`) | `mapping.canonical_mapping` (`000011`) | none exposed via HTTP yet (only used internally by resolver/repository) | none | Mismatch — table/columns (`canonical_mapping_id`, `source_entity_id`, `target_entity_id`) diverge from shared's `mapping_id`, `canonical_entity_id`, `external_reference_id`/`target_canonical_entity_id`, `scope_id` | No scope, direction, cardinality, confidence, or temporal columns on the table itself (`internal/repository/postgres.go` hardcodes these as constants when reading rows back, e.g. `m.ResolutionMode = "SINGLE"`) — the persisted data cannot actually represent the variability the contract requires | Partial — `canonical_test.go`/`repository_test.go` unit tests against in-memory fixture only | P1: schema rework per audit §2.2/§10.4 |
| **MappingScope** | `canonical-mapping.schema.json` §`mappingScope`; Mapping Model §10 (Proposed, informative) | not modelled as a Go type | `mapping.mapping_scope` (`000009`) — only `tenant_id`, `market_id`, `entity_type`, `scope_type`; none of the ~18 scope dimensions the shared schema defines (`legal_entity_id`, `operating_region_id`, `country`, `currency`, `locale`, `engine_id`, `environment`, etc.) | none | none | Mismatch — a small fraction of the contract's scope dimensions are represented | Cannot yet express the scope specificity/precedence rules the Mapping Model requires (Proposed doc, informative) or that `canonical-mapping.schema.json`'s `mappingScope` (accepted) defines | None | P2: expand `mapping_scope` columns to match `canonical-mapping.schema.json` before building real scope-precedence resolution |
| **Market** | `market.schema.json` | not modelled as a Go type | `market.market` table exists (`000006_markets.sql`, confirmed via migration test) | none | none | N/A — no Go code reads/writes it yet | Unimplemented (schema-only) | None | P2: implement once mapping/scope work lands; do not duplicate `Market` semantics into `mapping_scope.market_id` without a real FK |
| **DigitalEstate** | Mapping Model §12 (Proposed, informative); referenced by ADR-0001 (shared) §2.4 as a repository-owned concept, not a `baobab-cp` table per se | not modelled | `estate.digital_estate` table exists (`000007_digital_estates.sql`) | none | none | N/A | Unimplemented (schema-only); per ADR-0001 (shared), a digital estate is owned by its own repository — `baobab-cp` only needs a reference, not full estate metadata | None | P3: confirm intended scope with an ADR before building more than a reference column |
| **Engine / EngineInstance** | Mapping Model §13–14 (Proposed, informative) | not modelled as Go types beyond `resolver.EngineInstance` (used only by the resolver package, not backed by the same table) | `topology.engine` / `topology.engine_instance` (`000005`) | `ListActiveInstances` used internally by `internal/repository/postgres.go` | none | Partial — `code`/`region` vs. Mapping Model's `engine_key`/`deployment_region` (informative doc, not binding, but worth aligning for consistency) | Two independent Go representations exist (`domain` package has none; `resolver.EngineInstance` and the DB row shape differ) | Partial — `topology`-adjacent code has no dedicated unit tests beyond what `repository_test.go` exercises indirectly | P2: unify on one Go type for engine/engine instance |
| **Capability / CapabilityBinding** | Mapping Model §15–16 (Proposed, informative); temporal-exclusion pattern also required by `ADR-BCP-001` (local, Proposed) | `resolver.CapabilityBinding` | `capability.capability` (`000008`), `capability.capability_binding` (`000012`, **has** the `PRIMARY`/`ACTIVE` GiST exclusion constraint — the one part of this table set that already matches the accepted temporal-integrity principle) | none exposed via HTTP | none | Partial — `code` vs. `capability_key` | The `capability_binding_primary_excl` exclusion constraint is genuinely correct and matches ADR intent — call this out as a **positive** finding, not just a gap | Partial — `capability_test.go`(resolver)/`repository_test.go` unit tests only; no test proves the DB actually rejects an overlapping insert | P2: add a real-Postgres test proving the exclusion constraint fires (this is cheap and high-value) |
| **Context** | `context-resolution.schema.json`; Mapping Model §17 (Proposed, informative for the richer envelope) | `domain.ResolveContextRequest`/`ResolvedContext` (`internal/domain/context.go`); `internal/resolver/context.go` | context resolution reads `tenants`/`product_subscriptions` (the non-migrated tables, §2.1) for the live `/v1/context/resolve` path; a separate `audit.context_snapshot` table exists in the canonical migration set but is unused | `POST /v1/context/resolve` | none | Match on the fields the shared `context-resolution.schema.json` actually requires (`tenant_id`, `entity_id`, `lifecycle_status`, `product_id`, `entitled`, `cache_ttl_seconds`, `resolved_at`, `correlation_id`) | Fail-closed semantics are implemented (`contextPolicyDecision` denies on any non-active/non-entitled state) — a genuine, correct match to ADR-0004/shared's fail-closed requirement | Partial — `context_policy_test.go` unit-tests the decision function directly (good); no integration test runs it against a migrated DB | P0 (shared with Tenant row): fix the underlying migration gap; the decision logic itself does not need to change |
| **IsolationProfile** | Mapping Model §18 (Proposed, informative) | not modelled | `policy.isolation_profile` table exists (`000004_isolation_profiles.sql`) | none | none | N/A | Unimplemented (schema-only) | None | P3: no action until an engine adapter needs it |
| **Event envelope** | `contracts/events/v1/envelope.schema.json`; `control-plane/v1/event-envelope.schema.json` (references the same file); ADR-0004 | `internal/events.New` — validated constructor, fails closed on a bad type/missing correlation id/non-canonical tenant id | `messaging.outbox`/`messaging.inbox` tables exist (`000015_messaging.sql`) but type `tenant_id`/`aggregate_id` as `uuid`, incompatible with the `tn_`-prefixed tenant-provisioning aggregate identity; `outbox_events` (the table `store.go` actually writes to) is the non-migrated legacy table from §2.1 | none | constructed by `internal/events`, not yet wired to any write path | Match — envelope field names/shape now come from one validated constructor | Envelope construction is correct and schema-verified; transactional wiring is blocked on the `messaging.outbox` column-type mismatch above; no RabbitMQ publisher exists (no broker available to verify one against in this environment) | Covered (unit) + Covered (real-schema compatibility, `internal/events/contract_compatibility_test.go`) for construction; none for a write path (doesn't exist yet) | P1: fix `messaging.outbox`'s tenant_id/aggregate_id column types, wire `internal/events` into the tenant-provisioning transaction, then build a publisher against a real broker |
| **Idempotency (commands)** | `idempotency/v1/policy.yaml` | `api/router.go` `register()` handler; `store.go` `RegisterTenant` | `provisioning_operations.idempotency_key UNIQUE` (non-migrated table, §2.1) | `Idempotency-Key` header required on `POST /v1/tenants` | n/a | Partial — length-checked (16–128), character-pattern (`^[A-Za-z0-9][A-Za-z0-9._:-]*$`) not checked, and scoping is global rather than `(service, operation, tenant_id, key)` as the policy specifies | Replay of an identical request returns the original result (correct); replay with a different payload returns `409 IDEMPOTENCY_KEY_REUSED` (correct semantics, matches the policy's `IDEMPOTENCY_KEY_REUSED` code exactly) | None beyond what's implied by the (broken-at-runtime) tenant tests | P1: add pattern validation and scope the uniqueness constraint by tenant + operation, not key alone |
| **HTTP error shape** | `errors/v1/problem-details.schema.json` | `api/router.go`'s `problem()` helper | n/a | every error response | n/a | **Match** — `type`, `title`, `status`, `detail`, `code`, `correlation_id`, `retryable`, `application/problem+json` all present and correctly named | Conformant | Partial — exercised implicitly by handler tests, no dedicated schema-validation test | P3: add a test that validates a sample error response against the actual JSON Schema |
| **Access-token claims** | `access-token-claims.schema.json`; `security-policy.yaml` | `internal/auth/oidc.go` | n/a | Bearer token verification on every authenticated route | n/a | Match on required claims (`iss`,`sub`,`aud`,`exp`,`iat`,`jti`,`scope`,`actor_type`) referenced in `auth.Principal` | Appears conformant (not exhaustively re-audited against `oidc.go`'s full verification logic in this pass — flagged for a follow-up focused security review) | `oidc_test.go` exists | P3: dedicated follow-up audit of `internal/auth` against `security-policy.yaml`'s `failure_posture` table |

---

## Notes on this matrix

- Rows marked "Mapping Model §N (Proposed, informative)" cite the *aspirational*
  `Baobab Canonical Mapping Model.md` document, whose own header states `Status: Proposed
  Canonical Architecture`. It is used here only to give vocabulary and structure to
  concepts `baobab-cp` has already chosen to build toward (via its own, also-Proposed,
  `ADR-BCP-001`) — it is not being treated as an accepted external obligation the way
  ADR-0001–0005 and the `contracts/control-plane/v1/*.schema.json` files are.
- "DB representation" cites the migration file that actually creates the table, verified by
  reading the `.sql` file directly — not by trusting `ADR-BCP-001`'s prose description of
  what that migration "SHALL" contain, which in several cases (canonical registry, mapping,
  capability, engine topology) describes a materially richer table than what is actually
  migrated. Where the two disagree, this matrix follows the real `.sql` file.
- This matrix should be treated as a living document: re-run this comparison whenever
  `contracts.lock.yaml`'s pinned commit changes, or whenever a new migration is added.
